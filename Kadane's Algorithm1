long long maxSubarraySum(int arr[], int n)
{
   
    int maxsum=0, maxend=0;
    for(int i=0;i<n;i++)
    {
        maxend=maxend+arr[i];
        if(maxsum<maxend)
        {
            maxsum=maxend;
}
        if(maxend<0)
        {
            maxend=0;
}
        
}
    return maxsum;
}
