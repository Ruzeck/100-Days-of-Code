#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include<bits/stdc++.h>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */ 
    int n;
    cin>>n;
    int k;
   // cin>>k;
    int a=n*n;
    int arr1D[a]={'\0'};
   // cin>>k;
    int arr[n][n]={'\0'};
    for(int i=1;i<=n;i++)
    {
        for(int j=1;j<=n;j++)
        {
            arr[i][j]=i*j;
            
        }
        
    }
  /* for(int i=1;i<=n;i++)
    {
        for(int j=1;j<=n;j++)
        {
            cout<<arr[i][j]<<" ";
            
        }
        cout<<endl;
        
    }*/
   for (int i = 1; i <=n; i++) {
		for (int j = 1; j <= n;j++) {
			// mapping 1D array to 2D array
			arr1D[(i * n)+ j] = arr[i][j];
			//sort (arr1D,a);
		}
		
	}
	//sort(arr1D,arr1D+a+1);
	//cout<<arr1D[k];
	for(int i=1;i<=n*n;i++)
	{
	    cout<<arr1D[i]<<" ";
	}
    
    return 0;
}
